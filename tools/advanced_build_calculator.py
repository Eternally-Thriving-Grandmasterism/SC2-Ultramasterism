# Ultramaster Build Simulator v2.0
def main():
    matchup = input("Enter matchup (e.g., PvZ): ")
    if matchup.lower() == "pvz":
        print("Proxy Immortal Timing Loaded — Drone K Eternal")
        # Add timings, supply blocks, etc.
    # Expand with dict of all matchups

main()
